galaxy_info:
  author: Roberto Cardenas Isla
  description: Add new k8s node to the cluster started with robedevops.ansible_k8s_master
  company: Own project

  license: BSD

  min_ansible_version: 2.7

  platforms:
  - name: EL
    versions:
    - 7

  galaxy_tags:
    - k8s
    - kubernetes
    - node
    - worker
    - cluster
    - docker
    - containers

dependencies:
  - rodedevops.ansible_k8s_master